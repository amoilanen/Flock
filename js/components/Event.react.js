var React = require('react');

var Event = React.createClass({

  render: function() {
    return (
      /* jshint ignore:start */
      <div className="event">
        <p>Event tab active</p>
      </div>
      /* jshint ignore:end */
    );
  }
});

module.exports = Event;